import { PriorizarTareasPipe } from './priorizar-tareas.pipe';

describe('PriorizarTareasPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorizarTareasPipe();
    expect(pipe).toBeTruthy();
  });
});
